const { resolve } = require('path');

module.exports = {
  config: resolve(__dirname, 'src', 'config', 'database.js'),// Config do banco de dados
  'migrations-path': resolve(__dirname, 'src','database', 'migrations'),// Indica onde está as migrations
  'seeders-path': resolve(__dirname, 'src', 'database', 'seeds'),
};
